return {
    Type = 0,
    Name = "",
    Desc = "",
    IconPath = "",
    UsableJobs = {
        1,
    },
    UsableGenders = {
        1
    },
    ConsumableInfo = {
        HpRecovery = 0,
        HpRecoveryRate = 0.0,
        MpRecovery = 0,
        MpRecoveryRate = 0.0,
    },
    ResMgrEquDataPath = "clothes/swordman/belt/renewal",
}    local job = self.player.identity.Job
    if not Common.IsArticleInfoFitForJob(itemInfo, job) then
        print("UiModel:mountEquipment(index, itemInfo)", "equ is not fit for job:", job)
        return
    end
