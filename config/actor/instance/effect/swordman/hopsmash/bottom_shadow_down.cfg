-- 崩山击落地时地面出现的冲击波阴影下方半部分
return {
    aspect = {
        layer = {
            type = "frameani",
            path = "$A"
        },
        order = 10
    },
    effect = {
        positionType = "normal",
        lockRate = true,
        lockStop = true,
        lockState = true,
        lockDirection = true
    }
}