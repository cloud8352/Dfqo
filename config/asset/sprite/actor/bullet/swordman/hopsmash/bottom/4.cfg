-- 只使用其碰撞盒参数，不使用显示数据
return {
    ox = 93 - 50,
    oy = 19,
    blendmode = "add",
    collider = {
        attack = { -- 碰撞盒中心和距离实体位置距离ox == x + w / 2
            {
                x = -15,
                y1 = -65,
                y2 = 65,
                z = 0,
                w = 270,
                h = 25
            }
        }
    }
}